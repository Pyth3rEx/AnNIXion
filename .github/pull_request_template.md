## Changelog

<!-- This text becomes the GitHub Release notes when this PR is merged.
     Write it for end users, not reviewers.

     One change per bullet, on one line, prefixed with its section name.
     Reference issues with a closing keyword — `Closes #12`, not `(#12)` —
     or GitHub will not close them on merge.

     On a `dev → main` release PR this form is enforced by CI. Use
     .github/RELEASE_TEMPLATE.md there, and check a draft with:
       python3 .github/scripts/check-release-form.py my-draft.md -->

### Added
- Added: 

### Changed
- Changed: 

### Removed
- Removed: 

### Fixed
- Fixed: 

## Issues closed on merge

- Closes #

---

**Version:** `OLD` → `NEW`
